module Tailwind.Accessibility exposing (..)

import Sky.Core.Prelude exposing (..)
import Tailwind.Internal.Classes exposing (cls)

srOnly : ( String, String )
srOnly =
    cls "sr-only"

notSrOnly : ( String, String )
notSrOnly =
    cls "not-sr-only"
